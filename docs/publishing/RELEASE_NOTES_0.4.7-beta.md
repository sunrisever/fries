# Fries v0.4.7-beta

This beta refreshes the public release page with a clean multi-platform tag after the previous stale-tag mismatch.

## Highlights

- Reissued the beta line so release assets can line up with the actual version number.
- Kept Linux in the official release flow with `AppImage`, `deb`, and `rpm` targets.
- Continued to ship Windows and macOS assets alongside the latest heatmap threshold-mode improvements.

## Platform Notes

- Windows ships as installer, portable, and unpacked directory.
- Linux ships as `AppImage`, `deb`, and `rpm`.
- macOS continues to ship as an **unsigned beta** `zip / dmg`.
